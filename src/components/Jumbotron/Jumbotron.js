import React from "react";
import './Jumbotron.css';

const Jumbotron = () => (
  <div className="jumbotron">
    <h1>Memory</h1>
    <br />
    <p>
      Try to click on every image, but don't click on the same image twice!
    </p>
  </div>
);

export default Jumbotron;
